import React, { useState } from 'react'
import './CardItem.css'

function CardItem({cardName, minPledge, description, limitNumberEntry}) {
    const [limitNumber, setLimitNumber] = useState(limitNumber)

    return (
        <>
        </>
    )
}

export default CardItem
